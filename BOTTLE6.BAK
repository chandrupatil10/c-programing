#include<stdio.h>
void main(){
int  *ptr = (int *)malloc(100000000 * sizeof(int),NULL);
clrscr();
if(ptr ==NULL){
printf("memory allocations failed\n");
}else{
printf("memory allocations succeded\n");
free(ptr);
}
}